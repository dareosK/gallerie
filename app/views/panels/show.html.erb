<div class="container-fluid d-flex mt-5">
	<div class="col-8 m-2">
		<div class="panny ml-2">
			<%= cl_image_tag(@panel.wall.key, transformation: { width: 500, height: 500, crop: 'fill'} )%>
		</div>
	</div>
	<div class="col-4 m-2 grid">
		<% @artworks.each do |a| %>
				<%= cl_image_tag(a.photo.key, transformation: { width: 100, height: 100, crop: 'fit'})%>
		<% end %>
	</div>
</div>

<!-- ticket 