<div class="container d-flex mt-5">
	<div class="col-8 m-2">
		<div class="panny ml-2">
			<%= @panel %>
		</div>
	</div>
	<div class="col-4 m-2 grid">
		<% @artworks.each do |a| %>
				<%= cl_image_tag(a.photo.key, transformation: { width: 100, height: 100, crop: 'fill'})%>
		<% end %>
	</div>
</div>

<!-- ticket 