<h1>All shows</h1>

<div class='container'>
  <div class='masonry-grid'>
    <% @shows.each do |s| %>
      <div class='masonry-grid-item'><%= image_tag(s.photo) %></div>
    <% end %>
  </div>
</div>


